<Page
    x:Class="DataBinding_and_ViewTransition.GridView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:DataBinding_and_ViewTransition"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid x:ConnectionId='1' Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"                           >
        <Grid.RowDefinitions>
            <RowDefinition Height="1*"></RowDefinition>
            <RowDefinition Height="0.9*"></RowDefinition>
            <RowDefinition Height="1*"></RowDefinition>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"></ColumnDefinition>
            <ColumnDefinition Width="5*"></ColumnDefinition>
            <ColumnDefinition Width="2*"></ColumnDefinition>
        </Grid.ColumnDefinitions>

        <GridView x:Name="bookGrid" Grid.Row="1" Grid.Column="1">
            <GridView.ItemTemplate>
                <DataTemplate>
                    <Grid x:ConnectionId='4' HorizontalAlignment="Left" Width="200" Height="250"                                                         Margin="5,5,5,5">
                        <Border Background="{ThemeResource ListViewItemPlaceholderBackgroundThemeBrush}">
                            <Image Source="{Binding CoverUri}" Stretch="UniformToFill"/>
                        </Border>
                        <StackPanel VerticalAlignment="Bottom" Background="{ThemeResource ListViewItemOverlayBackgroundThemeBrush}" Visibility="Collapsed">
                            <TextBlock Text="{Binding Title}" Foreground="{ThemeResource ListViewItemOverlayForegroundThemeBrush}" Style="{StaticResource TitleTextBlockStyle}" Margin="5,5,5,0" FontSize="19"/>
                            <TextBlock Text="{Binding Author}" Foreground="{ThemeResource ListViewItemOverlaySecondaryForegroundThemeBrush}" Style="{StaticResource CaptionTextBlockStyle}" TextWrapping="NoWrap" Margin="0,0,5,0" HorizontalAlignment="Right" VerticalAlignment="Top"/>
                            <TextBlock Text="{Binding Year}" Foreground="{ThemeResource ListViewItemOverlaySecondaryForegroundThemeBrush}" Style="{StaticResource CaptionTextBlockStyle}" TextWrapping="NoWrap" Margin="0,0,5,10" HorizontalAlignment="Right" VerticalAlignment="top"/>
                        </StackPanel>
                    </Grid>
                </DataTemplate>
            </GridView.ItemTemplate>
        </GridView>
        <Button x:ConnectionId='2' Content="Button" HorizontalAlignment="Left" Margin="63,59,0,0" VerticalAlignment="Top" Style="{ThemeResource NavigationBackButtonNormalStyle}" RenderTransformOrigin="-0.22,0.366"                      Grid.Column="0"/>
        <TextBlock Grid.Column="1" HorizontalAlignment="Center" TextWrapping="Wrap" VerticalAlignment="Center" FontSize="24">
        	<Run Text="2. "/>
        	<Run Text="Use "/>
        	<Run Text="GridView"/>
        	<Run Text=" &amp; View Transition"/>
        </TextBlock>

        <ListView x:Name="bookList" Grid.Row="1" Grid.Column="1" Visibility="Collapsed" Grid.RowSpan="2" Width="240">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid x:ConnectionId='3' HorizontalAlignment="Left" Width="200" Height="250"                                                         Margin="5,5,5,5">
                        <Border Background="{ThemeResource ListViewItemPlaceholderBackgroundThemeBrush}">
                            <Image Source="{Binding CoverUri}" Stretch="UniformToFill"/>
                        </Border>
                        <StackPanel VerticalAlignment="Bottom" Background="{ThemeResource ListViewItemOverlayBackgroundThemeBrush}" Visibility="Collapsed">
                            <TextBlock Text="{Binding Title}" Foreground="{ThemeResource ListViewItemOverlayForegroundThemeBrush}" Style="{StaticResource TitleTextBlockStyle}" Margin="5,5,5,0" FontSize="19"/>
                            <TextBlock Text="{Binding Author}" Foreground="{ThemeResource ListViewItemOverlaySecondaryForegroundThemeBrush}" Style="{StaticResource CaptionTextBlockStyle}" TextWrapping="NoWrap" Margin="0,0,5,0" HorizontalAlignment="Right" VerticalAlignment="Top"/>
                            <TextBlock Text="{Binding Year}" Foreground="{ThemeResource ListViewItemOverlaySecondaryForegroundThemeBrush}" Style="{StaticResource CaptionTextBlockStyle}" TextWrapping="NoWrap" Margin="0,0,5,10" HorizontalAlignment="Right" VerticalAlignment="top"/>
                        </StackPanel>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>


    </Grid>
</Page>

